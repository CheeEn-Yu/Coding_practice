#include <stdio.h>
int main(){
    int n,m;
    scanf("%d", &n);
    int f[n];
    for (int i = 0; i < n; i++) scanf("%d", &f[i]);
    scanf("%d", &m);
    int g[m];
    for (int i = 0; i < m; i++) scanf("%d", &g[i]);
    
    return 0;
}