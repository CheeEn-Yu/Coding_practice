#include<stdio.h>

int main(){
    int N,m;
    scanf("%d%d",&N,&m);
    int data[N];
    for (int i = 0; i < N; i++)
        scanf("%d",&data[i]);
    int max = 0;
    for(int i = 0; i < N; i++){
        
    }

    return 0;
}